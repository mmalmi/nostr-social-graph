[package]
name = "nostr-social-graph-heed"
version = "0.1.2"
description = "LMDB-backed Nostr social graph backend implementing the shared runtime trait"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage = "https://github.com/mmalmi/nostr-social-graph"
readme = "../../README.md"

[dependencies]
heed.workspace = true
nostr-social-graph = { version = "0.1.2", path = "../nostr-social-graph" }
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
